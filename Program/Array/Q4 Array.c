int main()
{

    int a[1000],E[j],O[k],i,k;
    printf("Enter the size of array:\n");
    scanf("%d", &n);
    for(i=0;i<n;i++)
    {
        scanf("%d", &a[i]);
        if(a[i]/2==0)
        {
            E[j]=a[i];
            j++;
        }
        else
        {
            o[k]=a[i];
            k++;
        }
        printf("Even Array \n");
        for(i=0;i<=j-1;i++)
            printf("%d", a[i]);

        printf("Odd Arrat\n");
        for(i=0;i<)
            printf("%d", o[k]);
    }

}
